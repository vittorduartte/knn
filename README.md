# Algoritmo KNN c/ Holdout e Cross Validation: K-Fold, Repeated K-Fold e Leave One Out

## Introdução
## Instalação
a. Utilizando a instância global do Python:
```shell
git clone
cd knn
pip install -r requirements.txt
```
b. Utilizando o gerenciador de ambientes Pipenv:
```shell
cd knn
pipenv install
pipenv shell
```
